{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "npm run localhost",
      "name": "Localhost preview (port 8080)",
      "request": "launch",
      "type": "node-terminal"
    }
  ]
}
